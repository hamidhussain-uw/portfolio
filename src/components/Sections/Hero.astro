---
import { HeroAvatarEntrance } from '../UI/HeroAvatarEntrance';
import { HeroContentStagger } from '../UI/HeroContentStagger';
import { HeroExpertiseMotion } from '../UI/HeroExpertiseMotion';
import { HeroMeshBackground } from '../UI/HeroMeshBackground';
---

<section
	id="about"
	class="relative flex min-h-[min(92dvh,52rem)] flex-col justify-center overflow-hidden pt-[max(6rem,calc(3.5rem+env(safe-area-inset-top,0px)+1rem))] pb-16 sm:pb-20 md:min-h-[min(88dvh,56rem)] md:pt-28"
	aria-labelledby="hero-heading"
>
	<div class="pointer-events-none absolute inset-0 z-0">
		<HeroMeshBackground client:load />
	</div>

	<div class="relative z-10 mx-auto w-full max-w-design-content px-5 sm:px-6">
		<div
			class="flex flex-col items-center gap-8 md:flex-row md:items-start md:gap-10 lg:gap-12 xl:gap-14"
		>
			<HeroAvatarEntrance client:visible />

			<div class="min-w-0 w-full flex-1 md:max-w-none md:pt-1">
				<HeroContentStagger client:visible />

				<HeroExpertiseMotion client:visible />
			</div>
		</div>
	</div>
</section>
